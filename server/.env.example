# Server
PORT=5000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/rmc_billing
JWT_ACCESS_SECRET=change_me
JWT_REFRESH_SECRET=change_me
COOKIE_DOMAIN=localhost
CLIENT_ORIGIN=http://localhost:5173

# Mail
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="RMC Billing <no-reply@rmc.local>"

# SMS (stub for now)
SMS_PROVIDER=stub

# Storage (local for now; swap to S3 later)
UPLOAD_DIR=./uploads
